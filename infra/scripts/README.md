# Whitelisted Scripts

Place pre-vetted scripts here for the ShellAdapter. No arbitrary shell execution is allowed. Use parameters and environment variables for configuration; avoid secrets in files.
